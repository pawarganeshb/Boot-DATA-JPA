import lombok.Data;

@Entity
@Data
@Table
public class User {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    private String bio;

    @OneToOne(cascade = CascadeType.ALL) // Automatically handles persistence for Profile
    private Profile profile; // Profile reference in User (parent)
}
